#include <stdio.h>
#include <stdlib.h>

int N;
int	p[500], f[500], s[500], v[500];
int ans[5]{999, 999, 999, 999, 999};
int main(){
	scanf("%d", &N);
	for(int i=0; i<N; i++){
		scanf("%d %d %d %d %d", &p[i], &f[i], &[i], &p[i], &p[i]);
		
	}
}
